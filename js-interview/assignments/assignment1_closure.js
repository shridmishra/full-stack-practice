/**
 * Assignment 1 — Closures & state
 *
 * Implement `makeCounter(initial)` which returns an object with three methods:
 * - increment(): increments internal counter and returns new value
 * - decrement(): decrements and returns new value
 * - value(): returns current value
 *
 * The internal counter must be private (closure) and independent across calls.
 */

// Assignment 1 — Closures & state
// TODO: implement `makeCounter(initial)` that returns an object with
// methods: increment(), decrement(), value().
// See `assignment1_closure.md` for references.

function makeCounter(/* initial = 0 */) {
  throw new Error('Not implemented');
}

module.exports = { makeCounter };
