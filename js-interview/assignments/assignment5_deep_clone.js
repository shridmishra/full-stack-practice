/**
 * Assignment 5 — deepClone
 *
 * Implement `deepClone(value)` that creates a deep copy of common JS values:
 * - primitives are returned as-is
 * - handles Arrays, plain Objects, Date, RegExp, Map, Set
 * - preserves shared references and handles circular structures using WeakMap
 */

// Assignment 5 — deepClone
// TODO: implement `deepClone(value)` that handles arrays, objects, Date,
// RegExp, Map, Set and circular references. See `assignment5_deep_clone.md`.

function deepClone(/* value */) {
  throw new Error('Not implemented');
}

module.exports = { deepClone };
