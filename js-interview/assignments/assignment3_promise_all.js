/**
 * Assignment 3 — Reimplement Promise.all
 *
 * Implement `myPromiseAll(promises)` that behaves like `Promise.all`:
 * - accepts an iterable of values/promises
 * - returns a Promise that resolves to an array of resolved values (in order)
 * - rejects if any input promise rejects (with the rejection reason)
 */

// Assignment 3 — Reimplement Promise.all
// TODO: implement `myPromiseAll(iterable)` which behaves like `Promise.all`.
// See `assignment3_promise_all.md` for references.

function myPromiseAll(/* iterable */) {
  throw new Error('Not implemented');
}

module.exports = { myPromiseAll };
