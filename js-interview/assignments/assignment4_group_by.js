/**
 * Assignment 4 — groupBy (higher-order functions & array methods)
 *
 * Implement `groupBy(array, iteratee)` which groups array items by the key
 * produced by `iteratee`.
 * - iteratee can be a function (item => key) or a string (property name)
 */

// Assignment 4 — groupBy (higher-order functions & array methods)
// TODO: implement `groupBy(array, iteratee)` which groups items by the key
// produced by `iteratee` (function or property name).
// See `assignment4_group_by.md` for references.

function groupBy(/* array, iteratee */) {
  throw new Error('Not implemented');
}

module.exports = { groupBy };
