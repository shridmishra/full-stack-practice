import React from 'react';

/**
 * Assignment 5 — Refs, Performance & Optimization
 *
 * Implement:
 * - FocusInput: uses useRef to focus an input on button click
 * - MemoizedList: demonstrates React.memo to prevent unnecessary re-renders
 * - ExpensiveComputation: uses useMemo for expensive calculation
 */

export default function FocusInput(/* no props */) {
  throw new Error('Not implemented');
}

export const MemoizedList = null; // replace with React.memo wrapped component

export function ExpensiveComputation(/* no props */) {
  throw new Error('Not implemented');
}
