import React from 'react';

/**
 * Assignment 4 — Context, Custom Hooks & Advanced Patterns
 *
 * Implement:
 * - ThemeContext & ThemeProvider component that tracks theme state and provides a toggle function
 * - useTheme custom hook that consumes the context (throws error if used outside provider)
 * - ThemeToggle component that uses the custom hook to display current theme and toggle button
 */

export const ThemeContext = null; // replace with createContext

export function ThemeProvider(/* { children } */) {
  throw new Error('Not implemented');
}

export function useTheme(/* no args */) {
  throw new Error('Not implemented');
}

export default function ThemeToggle(/* no props */) {
  throw new Error('Not implemented');
}
