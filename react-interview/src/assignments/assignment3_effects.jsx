import React from 'react';

/**
 * Assignment 3 — Effects, Data Fetching & Custom Hooks
 *
 * Implement these pieces:
 * - DataFetcher({ url }) — useEffect-based fetcher that shows loading/error/data
 * - useFetch(url) — a custom hook returning { data, loading, error }
 * - Cleanup and cancellation behavior (component unmounting)
 *
 * Export default one component (e.g. DataFetcher) and provide the named hook.
 */

export default function DataFetcher(/* { url } */) {
  throw new Error('Not implemented');
}

export function useFetch(/* url */) {
  throw new Error('Not implemented');
}
